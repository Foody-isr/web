.next
.turbo
node_modules
.env.local
.env
out
*.log
*.tsbuildinfo
.vercel

# Note: lib/themes/generated/ is *committed* — it's the build output of
# design-tokens/build.ts. The matching foodyserver internal/themes/catalog.json
# is also committed. CI runs `design-tokens/build.ts --check` to verify the
# committed files match the source JSON. Do not gitignore.
